export function resume() {}

export class One {
    bar = "abc";

    priv = null;

    constructor(foo, bar) {
        this.bar = yield(this.test(resume());
    }

    test(foo, next) {}

next()



    foo() {}
}

/*
//@ sourceMappingURL=one.map
*/
